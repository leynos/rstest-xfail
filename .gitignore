target/
**/*.rs.bk
__pycache__/

# Generated by local builds; this is a library crate, so the lock file is not
# a build input for downstream consumers and is deliberately left untracked.
Cargo.lock

# Local spelling-policy caches and helper tool state
.coverage
.pytest_cache/
.uv-cache/
.uv-tools/
.typos-oxendict-base.json
.typos-oxendict-base.toml
